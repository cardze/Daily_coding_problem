# Daily Coding Problem Subscription Configuration

# Email address to use for subscription
# You can also set this as an environment variable: export DCP_EMAIL=your.email@example.com
DCP_EMAIL=your.email@example.com

# Email password for checking inbox (use app-specific password for Gmail/Yahoo/etc.)
# Required only if you want to check for new problems in your email
DCP_EMAIL_PASSWORD=your_app_specific_password

# IMAP server (optional, auto-detected based on email domain if not provided)
# Examples: imap.gmail.com, imap.mail.yahoo.com, imap-mail.outlook.com
DCP_IMAP_SERVER=imap.gmail.com
